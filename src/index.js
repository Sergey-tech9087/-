import React from 'react';
import ReactDOM from 'react-dom';

import App from './components_/app_/app_';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
